---
name: "Pull Request: Validate"

on:
  pull_request:
    branches: ["master"]
    types: ["opened", "synchronize", "reopened"]

concurrency:
  group: ${{ github.head_ref }}-pr-validate
  cancel-in-progress: true

jobs:
  simple-checks:
    uses: drag0n141/containers/.github/workflows/simple-checks.yaml@fd06ddcc3fcb5b209577f7762beb728ed13dbbb2 # master

  get-changed-images:
    uses: drag0n141/containers/.github/workflows/get-changed-images.yaml@fd06ddcc3fcb5b209577f7762beb728ed13dbbb2 # master

  build-images:
    needs: ["simple-checks", "get-changed-images"]
    if: ${{ needs.get-changed-images.outputs.addedOrModified == 'true' }}
    uses: drag0n141/containers/.github/workflows/build-images.yaml@fd06ddcc3fcb5b209577f7762beb728ed13dbbb2 # master
    secrets: inherit
    with:
      appsToBuild: "${{ needs.get-changed-images.outputs.addedOrModifiedImages }}"
      pushImages: false
      sendNotifications: false
